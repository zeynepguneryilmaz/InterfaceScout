fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0
numpy>=1.26
biopython>=1.83
pdb2pqr>=3.6.1
# APBS binary: pip wheels exist only for Linux/macOS.
# On Windows this line is skipped (marker) and run_local.bat downloads APBS instead.
apbs-binary>=3.4.1.2 ; platform_system != "Windows"
